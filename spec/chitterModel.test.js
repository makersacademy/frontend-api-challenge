const ChitterModel = require('../lib/chitterModel');

describe('Chitter class', () => {
  let chitter;

  beforeEach(() => {
    chitter = new ChitterModel();
  });

  it('does nothing', () => {

  });
})