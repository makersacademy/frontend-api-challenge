{
  "name": "chitter_frontend",
  "lockfileVersion": 2,
  "requires": true,
  "packages": {},
  "devDependencies": {
    "jest": "^27.4.7"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild index.js --bundle  --outfile=bundle.js --watch"
  }
}
